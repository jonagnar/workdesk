# templates

Copier template source for scaffolding new project repos. Each feature
(git hooks, database service, podman compose, CLAUDE.md, mise.toml) is a
toggleable prompt at generation time — a project only ends up with the
files it opted into.

Not yet built. Next step: `copier` template skeleton with a `copier.yml`
defining the toggles.
