# Everything you clone or scaffold lives in repos/ and is never tracked here.
/repos/

# Never commit unencrypted secrets (encrypted *.enc.* files are fine)
*.key
*.pem
.env
.env.*
!.env.enc.*
!.env.example

# OS / editor noise
.DS_Store
*.swp
